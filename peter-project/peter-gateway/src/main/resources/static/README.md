Copy the built frontend files here.

For a Vite or Vue project, run the frontend build command, then copy everything from the generated `dist` directory into this folder. The final structure should include:

```text
static/index.html
static/assets/...
```
